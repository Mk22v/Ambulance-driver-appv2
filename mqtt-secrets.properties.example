# Copy this file to mqtt-secrets.properties (gitignored) and fill in your
# MQTT broker details. These are baked into the app at build time and are
# never shown on any screen.

mqtt.host=YOUR_CLUSTER.s1.eu.hivemq.cloud
mqtt.port=8883
mqtt.username=YOUR_USERNAME
mqtt.password=YOUR_PASSWORD
